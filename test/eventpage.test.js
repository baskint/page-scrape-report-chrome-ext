'use strict';
var expect = chai.expect;

describe('Event page ', function () {

  beforeEach(function () {
    // before hook
  });

  it('should return true', function () {
    var tab = new Object();
    var spy = sinon.spy();

    //chrome.windows.create(spy);

    //psaEvent.start(tab);


  });

  afterEach(function () {
    // after hook
  });
});
